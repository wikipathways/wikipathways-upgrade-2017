* Run setup-site.sh to set up the site after doing an initial checkout
* wikipathways.username should be replaced with a "proper" mwjs fetching of the user name.  you can find this in mw.config.get("wgUserName"
* Modify MediaWiki:Common.js so that it executes after things are loaded
** Surround everything with
<syntaxhighlight lang="javascript">
mw.loader.using( ['mediawiki.user'] ).done( function() {
...
} );
</syntaxhighlight>
** Change addOnloadHook() to $(document.ready( ...
<syntaxhighlight lang="javascript">
$(document.ready( function() { CustomizeModificationsOfSidebar(); } );
</syntaxhighlight>
* Moving style attributes from elements surrounding pathways to wpi.Pathway module
** Found at extensions/GPML/modules/Pathway.*
** Allows de-coupling style and behavior from content
also, stop embedding js and embrace newer MW's JS dependencies
